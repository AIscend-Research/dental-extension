# Core deps for the team's own work: degradation pipeline, dataset/annotation
# handling, confidence-head logic, and evaluation metrics.
# NONE of this needs a GPU or detectron2 -- install it and start working today.
numpy>=1.24
opencv-python-headless>=4.8
albumentations>=1.4      # extra augmentations if you want them alongside the custom pipeline
scikit-learn>=1.3        # metric helpers
pandas>=2.0
matplotlib>=3.7
tqdm>=4.66
pyyaml>=6.0
huggingface_hub>=0.23    # to pull the DENTEX dataset
pytest>=8.0
pycocotools>=2.0.7       # coco_map()/detection eval -- no torch/detectron2 needed for this
